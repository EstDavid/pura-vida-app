import { View } from 'react-native';
import { Text } from 'react-native-paper';
import AppBar from '../AppBar';

const About = () => {
    return (
        <View>
            <AppBar title="About Pura Vida" />
            <Text>This About</Text>
        </View>
    );
};

export default About;