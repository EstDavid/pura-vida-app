import { Text, View } from 'react-native';
import AppBar from '../AppBar';

const Home = () => {
    return (
        <View>
            <AppBar title="Home" />
            <Text>This is Pura Vida!!</Text>

        </View>
    );
};

export default Home;