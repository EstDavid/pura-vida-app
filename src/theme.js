import { Platform } from 'react-native';

const theme = {
    appBarLogoContainer: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        flex: 1,
        width: 40,
        height: 40
    }
};

export default theme;